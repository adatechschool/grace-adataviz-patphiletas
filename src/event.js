

// // export const displayEvents = async function() {
//     // On attend que les données soient chargées
//     const response = await fetch(recupererDonnees());
//     // On récupère la div où mettre le résultat
//     const data = await response.json();
//     const container = document.getElementById("events");
//     // Si pas de données → on arrête
//     if (!data || !data.results) {
//         container.textContent = "Aucun événement trouvé (ou erreur API).";
//         return;
//     }
// ;


